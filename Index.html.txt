<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1.0" />
  <title>Ghosty Clothing</title>
  <style>
    /* Page setup */
    :root {
      --bg: #0a0a0a;
      --panel: #141414;
      --text-main: #ffffff;
      --text-dim: #888888;
      --accent: #9a7bff;
      --border: #2a2a2a;
    }

    * {
      box-sizing: border-box;
      -webkit-font-smoothing: antialiased;
    }

    body {
      margin: 0;
      background-color: var(--bg);
      color: var(--text-main);
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Inter", Roboto, "Helvetica Neue", sans-serif;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    header {
      border-bottom: 1px solid var(--border);
      background: radial-gradient(circle at 20% 20%, rgba(154,123,255,0.12) 0%, rgba(0,0,0,0) 60%);
      padding: 1.25rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .brand {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      font-weight: 600;
      font-size: 1rem;
      line-height: 1;
      letter-spacing: 0.05em;
      text-transform: uppercase;
      color: var(--text-main);
    }

    .logo-circle {
      width: 36px;
      height: 36px;
      border-radius: 999px;
      background: radial-gradient(circle at 30% 30%, #ffffff 0%, #aaaaaa 30%, #000000 70%);
      border: 1px solid #ffffff22;
      box-shadow: 0 0 20px rgba(154,123,255,0.6), 0 0 60px rgba(154,123,255,0.15);
      position: relative;
    }

    .logo-circle::after {
      /* ghost face (eyes) */
      content: "";
      position: absolute;
      left: 50%;
      top: 45%;
      transform: translate(-50%, -50%);
      width: 14px;
      height: 6px;
      background: radial-gradient(circle,#000 0%,#000 40%,transparent 41%);
      border-radius: 999px;
      box-shadow:
        -6px 0 0 0 #000,
        6px 0 0 0 #000;
    }

    nav {
      display: flex;
      gap: 1rem;
      font-size: 0.8rem;
      color: var(--text-dim);
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }

    nav a {
      color: var(--text-dim);
      text-decoration: none;
    }

    nav a:hover {
      color: var(--text-main);
    }

    main {
      flex: 1;
      padding: 3rem 2rem 5rem;
      max-width: 1200px;
      width: 100%;
      margin: 0 auto;
      display: grid;
      gap: 2rem;
    }

    .hero {
      background: radial-gradient(circle at 50% 0%, rgba(154,123,255,0.18) 0%, rgba(0,0,0,0) 70%);
      border: 1px solid var(--border);
      background-color: var(--panel);
      border-radius: 16px;
      padding: 2rem;
      min-height: 220px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      box-shadow:
        0 30px 120px rgba(154,123,255,0.15),
        0 4px 20px rgba(0,0,0,0.8);
    }

    .tagline {
      font-size: clamp(1.8rem, 2vw, 2.2rem);
      font-weight: 600;
      line-height: 1.2;
      color: var(--text-main);
      text-transform: uppercase;
      letter-spacing: -0.03em;
    }

    .subtext {
      max-width: 40ch;
      margin-top: 0.75rem;
      font-size: 0.9rem;
      line-height: 1.5rem;
      color: var(--text-dim);
    }

    .cta-row {
      display: flex;
      flex-wrap: wrap;
      gap: 0.75rem;
      margin-top: 1.5rem;
    }

    .cta-btn {
      background: var(--accent);
      border: 0;
      color: #000;
      font-weight: 600;
      font-size: 0.8rem;
      padding: 0.75rem 1rem;
      line-height: 1;
      border-radius: 8px;
      cursor: pointer;
      box-shadow:
        0 10px 30px rgba(154,123,255,0.6),
        0 0 60px rgba(154,123,255,0.4);
    }

    .cta-btn.secondary {
      background: transparent;
      border: 1px solid var(--border);
      color: var(--text-main);
      box-shadow: none;
    }

    .grid-3 {
      display: grid;
      grid-template-columns: repeat(auto-fit,minmax(min(100%,220px),1fr));
      gap: 1rem;
    }

    .card {
      background-color: var(--panel);
      border: 1px solid var(--border);
      border-radius: 12px;
      min-height: 140px;
      padding: 1rem 1.25rem;
      display: flex;
      flex-direction: column;
      justify-content: center;
      position: relative;
    }

    .card-label {
      font-size: 0.7rem;
      line-height: 1rem;
      font-weight: 600;
      color: var(--accent);
      text-transform: uppercase;
      letter-spacing: 0.08em;
      margin-bottom: 0.5rem;
    }

    .card-headline {
      font-size: 0.9rem;
      line-height: 1.4rem;
      color: var(--text-main);
      font-weight: 500;
    }

    .card-desc {
      font-size: 0.8rem;
      line-height: 1.3rem;
      margin-top: 0.5rem;
      color: var(--text-dim);
    }

    footer {
      border-top: 1px solid var(--border);
      background: #0c0c0f;
      color: var(--text-dim);
      font-size: 0.7rem;
      padding: 2rem;
      text-align: center;
      line-height: 1.4rem;
      letter-spacing: 0.03em;
    }

    footer span.brand-min {
      color: var(--text-main);
      font-weight: 600;
    }

    @media (min-width: 768px) {
      main {
        grid-template-columns: 1fr 1fr;
        align-items: start;
      }
      .hero {
        grid-column: 1 / -1;
      }
    }

  </style>
</head>
<body>

  <header>
    <div class="brand">
      <div class="logo-circle"></div>
      <div>Ghosty Clothing</div>
    </div>
    <nav>
      <a href="#">Shop</a>
      <a href="#">Drops</a>
      <a href="#">About</a>
      <a href="#">Cart (0)</a>
    </nav>
  </header>

  <main>
    <!-- hero / headline -->
    <section class="hero">
      <div class="tagline">
        Ghosty Clothing
        <br />
        <span style="color:var(--accent);">Streetwear from the other side.</span>
      </div>
      <div class="subtext">
        This is a placeholder hero section. Add your first drop,
        a photo of your main hoodie, or an announcement banner here.
      </div>
      <div class="cta-row">
        <button class="cta-btn">Shop Collection</button>
        <button class="cta-btn secondary">Lookbook</button>
      </div>
    </section>

    <!-- cards / sections -->
    <section class="grid-3">
      <div class="card">
        <div class="card-label">Featured</div>
        <div class="card-headline">Drop 001 / "Afterlife Hoodie"</div>
        <div class="card-desc">Your first product highlight goes here. Colorway, vibe, story.</div>
      </div>

      <div class="card">
        <div class="card-label">Story</div>
        <div class="card-headline">What is Ghosty?</div>
        <div class="card-desc">Write 2–3 sentences about the brand identity. Why it exists. Who it’s for.</div>
      </div>

      <div class="card">
        <div class="card-label">Updates</div>
        <div class="card-headline">Next drop soon</div>
        <div class="card-desc">Use this space like a notice board: pop-up events, preorder windows, collabs, etc.</div>
      </div>
    </section>
  </main>

  <footer>
    <div><span class="brand-min">Ghosty Clothing</span> © 2025. All rights reserved.</div>
    <div>IG / TikTok / Discord links go here.</div>
  </footer>

</body>
</html>